/*import 'dart:html';
import 'dart:math';
import 'dart:ui';

var j=Window.devic;
class BoxSize{
  String box;
  double height;
  BoxSize({required this.box, required this.height});
}

  List fn()
  {
    var pixelRatio= window;
    var logicalScreenSize= Window.;
      double scrHeight=physicalScreenSize.height;
      double scrWidth=physicalScreenSize.width;
      var timeBoxHeight=scrHeight *(.10);
      double pad_ing=max(0.0, scrHeight*0.01);
      double appBarHeight= scrHeight *(.10);
      double workBoxHeight= scrHeight-timeBoxHeight-pad_ing*2-appBarHeight;
    List<BoxSize>boxes=[
      BoxSize(box: "screen",height: scrHeight),
      BoxSize(box: "timeBox",height: timeBoxHeight),
      BoxSize(box: "workBox",height: workBoxHeight),
      BoxSize(box: "appBarBox",height: appBarHeight),
    ];
    return boxes;
  }
*/